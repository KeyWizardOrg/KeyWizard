<Window
    x:Class="Key_Wizard.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Key_Wizard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Microsoft Key Wizard"
    Activated="Window_Activated">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="MainGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0" TabFocusNavigation="Cycle" CornerRadius="{StaticResource OverlayCornerRadius}" KeyDown="MainGrid_KeyDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" x:Name="SearchRowDefinition"/>
            <RowDefinition Height="1*" x:Name="ResultsRowDefinition"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

            <TextBox x:Name="searchTextBox"
                    PlaceholderText="Key Wizard"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Margin="0,0,0,0"
                    Padding="36,16,0,0"
                    FontSize="16"
                    Background="Transparent"
                    TextWrapping="Wrap"
                    TextChanged="searchTextBox_TextChanged">
                <!-- Override the default style to remove the focus underline -->
                <TextBox.Style>
                    <Style TargetType="TextBox" BasedOn="{StaticResource DefaultTextBoxStyle}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Grid>
                                        <!-- Visual states for the TextBox -->
                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualState x:Name="Normal"/>
                                                <VisualState x:Name="Focused">
                                                    <!-- Remove the focus underline animation -->
                                                    <Storyboard/>
                                                </VisualState>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>

                                        <!-- Actual TextBox content -->
                                        <ScrollViewer x:Name="ContentElement"
                                          Padding="{TemplateBinding Padding}"
                                          Background="{TemplateBinding Background}"
                                          BorderThickness="0"
                                          IsTabStop="False"/>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TextBox.Style>
            </TextBox>
            <FontIcon
                    Glyph="&#xE71E;"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="16"
                    Margin="8,16,0,0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </Grid>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
            <ListView x:Name="shortcutsList"
                      Margin="8"
                      SelectionMode="None"
                      XYFocusKeyboardNavigation="Enabled"
                      SizeChanged="shortcutsList_SizeChanged">
                <!-- ListView templates remain the same -->
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Section">
                        <StackPanel Margin="0,0,0,0">
                            <!-- Section Header -->
                            <TextBlock Text="{x:Bind Name}"
                                       FontWeight="SemiBold"
                                       FontSize="12"
                                       Margin="0,0,0,4"/>
                            <!-- Section Items List -->
                            <ListView ItemsSource="{x:Bind Items}"
                                      SelectionMode="Single"
                                      KeyDown="ListView_KeyDown"
                                      IsItemClickEnabled="True"
                                      ItemClick="ListView_ItemClick">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="local:ListItem">
                                        <TextBlock Margin="0,0,0,0" >
                                            <Run Text="{x:Bind Section}" FontWeight="Normal"/>
                                            <Run Text="{x:Bind Prefix}" FontWeight="ExtraBold"/>
                                            <Run Text="{x:Bind Suffix}"/>
                                        </TextBlock>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                                <!-- Remove selection highlight -->
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    </Style>
                                </ListView.ItemContainerStyle>
                            </ListView>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</Window>