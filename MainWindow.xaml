<Window
    x:Class="Key_Wizard.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Key_Wizard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Microsoft Key Wizard">

    <Grid x:Name="MainGrid" HorizontalAlignment="Stretch" Margin="12" TabFocusNavigation="Cycle" CornerRadius="{StaticResource OverlayCornerRadius}" KeyDown="MainGrid_KeyDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Stretch">
            <TextBox x:Name="searchTextBox"
                    PlaceholderText="KeyWizard"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Margin="0,0,4,0"
                    Padding="28,0,0,0" 
                    Background="Transparent"
                    TextWrapping="Wrap"
                    TextChanged="searchTextBox_TextChanged"/>
            <FontIcon
                    Glyph="&#xE71E;"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="12"
                    Margin="8,0,0,8"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </Grid>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ListView x:Name="shortcutsList"
                      Margin="4"
                      SelectionMode="None"
                      XYFocusKeyboardNavigation="Enabled"
                      SizeChanged="shortcutsList_SizeChanged">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Section">
                        <StackPanel Margin="0,12">
                            <!-- Section Header -->
                            <TextBlock Text="{x:Bind Name}"
                                       FontWeight="SemiBold"
                                       FontSize="12"
                                       Margin="0,0,0,4"/>
                            <!-- Section Items List -->
                            <ListView ItemsSource="{x:Bind Items}"
                                      SelectionMode="Single"
                                      KeyDown="ListView_KeyDown"
                                      IsItemClickEnabled="True"
                                      ItemClick="ListView_ItemClick">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="local:ListItem">
                                        <TextBlock Margin="12,4" >
                                            <Run Text="{x:Bind Section}" FontWeight="Normal"/>
                                            <Run Text="{x:Bind Prefix}" FontWeight="ExtraBold"/>
                                            <Run Text="{x:Bind Suffix}"/>
                                        </TextBlock>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                                <!-- Remove selection highlight -->
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    </Style>
                                </ListView.ItemContainerStyle>
                            </ListView>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</Window>